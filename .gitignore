.venv/
__pycache__/
.pytest_cache/
*.pyc

# Trained weights are ~17 MB — keep them out of git, they come from Colab
backend/models/*.pt
# Runtime state the server writes (TTS cache, outbreak reports)
backend/var/

# Portable cloudflared binary (~55 MB) — re-download, don't commit
tools/

.ipynb_checkpoints/
.DS_Store
Thumbs.db
.vscode/
.idea/

*.env
venv/

# Datasets downloaded for fine-tuning (large, re-fetchable)
training/data/
